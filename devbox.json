{
  "packages": {
    "nodejs":         "21.3.0"
  },
  "shell": {
    "init_hook": [
      "npm install"
    ],
    "scripts": {
      "generate": "./from-docker.sh",
      "release": ["npm run release", "git push --follow-tags origin master"]
    }
  }
}
